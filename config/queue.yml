default: &default
  dispatchers:
    - polling_interval: 1
      batch_size: 500
  workers:
    - queues: "*"
      threads: 3
      processes: 1
      polling_interval: 0.1
    - queues: "images"
      threads: 2
      processes: 1
      polling_interval: 0.1

development:
  <<: *default

test:
  <<: *default

production:
  <<: *default
  workers:
    - queues: "default,downloads"
      threads: 5
      processes: 1
      polling_interval: 0.1
    - queues: "images"
      threads: 2
      processes: 2
      polling_interval: 0.1
